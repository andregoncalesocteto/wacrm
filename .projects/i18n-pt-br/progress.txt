# Ralph Progress Log — ralph/i18n-pt-br
Started: 2026-09-18
PRD: .projects/i18n-pt-br/prd.json (19 stories). No test-scenarios.json (acceptanceCriteria are the bar).

## Codebase Patterns
- Run project commands from the repo root (cwd can drift into .projects/…).
- Quality checks: `npm run typecheck`, `npm run lint`, `TZ=UTC npm test` (CI runs in UTC).
  `src/lib/dashboard/date-utils.test.ts` (mondayIndex, 2 tests) fails on machines with a non-UTC TZ
  (e.g. -03) because it builds `new Date("2026-05-18")` (UTC midnight). That is a pre-existing
  env issue, NOT part of this feature — do not edit that test; use TZ=UTC.
- Baseline before US-001: typecheck clean; lint 0 errors / 41 warnings (none from react/jsx-no-literals yet);
  `TZ=UTC npm test` fails only messages.test.ts pt/es "covers every en.json key" (6 keys each).
- i18n: next-intl v4.13.5; locale = NEXT_PUBLIC_APP_LOCALE (default en); catalogues in messages/{en,pt,es,ko}.json.
  Every new key must exist in all four catalogues (src/i18n/messages.test.ts parity + ICU args).
- Browser verification: a dev server in pt is expected at http://localhost:3100
  (`NEXT_PUBLIC_APP_LOCALE=pt npx next dev -p 3100`, started outside the stories, Supabase stack at
  http://supabase.localtest.me:8000 is up via docker compose). The docker app on :3000 is the pre-change `en` build.
  Test account created via signup (email autoconfirm is on).

---
## 2026-09-18 - US-001
- Added the 6 missing Contacts.importModal keys (resultInvalidPhone[_plural], failedRowsHeading, unknownReason, toastInvalidPhone[_plural]) to messages/pt.json and messages/es.json.
- Files changed: messages/pt.json, messages/es.json, prd.json, progress.txt
- **Learnings for future iterations:**
  - Keys are inserted in the same position as en.json (parity test only checks presence + ICU args, not order).
  - After this, `TZ=UTC npm test` is fully green (86 files / 1001 tests); lint stays at 41 warnings.
---
## 2026-09-18 - US-002
- quick-replies-manager.tsx now uses useTranslations("Settings.quickReplies") for all visible text (title, description, buttons, empty state, dialog, placeholders, confirm, toasts); 17 keys added to en/pt/es/ko.
- Files changed: src/components/settings/quick-replies-manager.tsx, messages/{en,pt,es,ko}.json, prd.json, progress.txt
- **Learnings for future iterations:**
  - Settings sections deep-link via `/settings?tab=<id>` (e.g. tab=quick-replies), not `section`.
  - en.json/ko.json have quirky whitespace; do NOT rewrite them via json.dump (huge diff) - insert the block textually.
  - Include `t` in useCallback deps when translating inside callbacks.
  - Browser check done in pt (list, empty state, dialog, validation toast). The nested InteractiveBuilder is a separate component (not this story).
---
